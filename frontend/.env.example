# Real API mode is the default. This base is proxied to FastAPI in development.
VITE_API_BASE_URL=/api/v1

# Development-only static fixture. It is visibly marked in the interface.
VITE_USE_FIXTURE=false
