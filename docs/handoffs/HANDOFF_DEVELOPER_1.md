# Handoff 1 — данные, агент качества и FastAPI

Ты — разработчик 1. Этот документ самодостаточен: он содержит контекст продукта, общий договор с разработчиком 2 и твой маршрут от пустого каркаса до интегрированного продукта. Второй документ — `HANDOFF_DEVELOPER_2.md`. Общая часть должна оставаться одинаковой в обоих документах.

Твоя ответственность: достоверно подготовить данные, обучить и проверить прогноз, определить границы оценки действий, предоставить агент качества и завершить HTTP/API-интеграцию. Ты не реализуешь второй координатор, PostgreSQL-схему или React.

## Общий контекст продукта — одинаковый для обоих разработчиков

### Цель и сроки

Команда из двух студентов IT-вуза делает продукт для победы в хакатоне. На разработку — 14 дней. Оба сначала занимаются Python-логикой, затем разработчик 1 завершает API, разработчик 2 делает веб. Не разбивайте работу на изолированные недельные проекты: каждый день интегрируйте текущий общий пример.

Продукт: советчик оператору производства дизельного топлива с интерактивным сравнением действий. Система принимает обновляющиеся измерения, прогнозирует развитие процесса при сохранении управляющих настроек, предлагает предпочтительное изменение и альтернативы. Оператор может ввести своё действие и получить такую же оценку.

Пользователь не должен постоянно загружать таблицы или выбирать исторические даты. В прототипе производственный поток воспроизводится последовательной подачей предоставленной истории. Режим «Воспроизведение истории» видим в UI. Подключение к промышленной системе и исполнение команд оборудованием в эту версию не входят.

Первая предметная область: гидроочистка 24-2000, основной показатель — сера на выходе. АВТ используется как дополнительный контекст сырья. Блендинг остаётся вне v1 без отдельного расширения задачи пользователем; организаторы допускают явно модельный контур и не предоставят дополнительные исходники. Качество после гидроочистки нельзя автоматически объявлять полным соответствием товарного топлива после блендинга.

Ожидаемый результат:
- прогноз сохранения настроек;
- модельная оценка нескольких изменений;
- фильтрация недопустимых вариантов;
- сравнение по качеству, тяжести режима и производственной эффективности;
- объяснение и сохранение решения;
- минималистичный рабочий экран в духе Apple: ясная типографика, спокойные поверхности, минимум лишних шагов.

### Реальное состояние репозитория

Срез на 2026-09-15, проверенный по коду и последним фактическим проверкам:
- Python 3.14; зависимости данных, модели, FastAPI и PostgreSQL уже в pyproject/lock.
- Реализованы contracts.py, протокол QualityAgent, общий synthetic fixture и JSON Schema.
- Реализованы загрузка/аудит и CLI prepare; train, evaluate, serve пока только план.
- Этап B реализован: evaluator с DI загружает валидируемую policy, проверяет управления, baseline, диапазоны/шаги/сочетания, свежесть, provenance, применимость, интервал серы и required checks. Тестовый QualityAgent только в tests. Реальные изменения пока запрещены из-за недостающих единиц/диапазонов/support. Coordinator ещё не ранжирует допустимые варианты (C–E) и явно сообщает об этом вместо ложного no_feasible_option.
- Есть compose.yaml (PostgreSQL 17), Alembic 0001, ORM, SnapshotRepository и DecisionRepository. Миграция, FK, rollback, идемпотентность save, JSON/TIMESTAMPTZ и reconnect проверены на отдельном временном PostgreSQL. Replay repository/advance и сервис API ещё не реализованы.
- config/controls.yaml содержит подтверждённые P8/T11/F19 с evidence; все available=false до получения численных единиц/шкал, train-диапазонов, шага и поддержки активной модели. config/constraints.yaml загружается evaluator, версии двух файлов сверяются. Ramp limits не выдуманы; необязательный переход/unknown cost не подменяются обязательными проверками.
- Реальной модели, SnapshotProvider, маршрутов FastAPI/OpenAPI и frontend пока нет. Артефакты прежнего prepare не гарантированно присутствуют в текущем checkout; их надо воспроизводить.
- Последние проверки после этапа B: Ruff check/format --check прошли; pytest — 85 passed, 2 skipped (PostgreSQL opt-in). PostgreSQL ранее проверен отдельно: 2 passed, в этапе B БД/миграции не менялись и эти тесты повторно не запускались.
- CSV развёрнуты через локально настроенный Git LFS; context/ содержательно не изменён. tmp/ — промежуточные материалы, не источник требований.
- Подробнее: [PREPARATION_DEVELOPER_2.md](PREPARATION_DEVELOPER_2.md), [SCENARIO_POLICY_V1.md](../SCENARIO_POLICY_V1.md). Ответы организаторов, включая ещё не внедрённые исправления подготовки первого: [ORGANIZER_CLARIFICATIONS.md](../ORGANIZER_CLARIFICATIONS.md).

Перед работой проверьте текущий git diff и AGENTS.md, если он появится. Не удаляйте чужие изменения, не переписывайте исходники context, не коммитьте секреты, сырые данные, обученные бинарные модели и большие производные файлы без отдельной договорённости. Не создавайте свои копии чужих модулей при отсутствии готовой реализации: используйте согласованный интерфейс и тестовый двойник.

### Материалы, которые необходимо прочитать

Все пути ниже — относительно корня репозитория.

| Источник | Назначение и особенности |
|---|---|
| `context/ТЗ_нефтекод.pdf` | Обязательный цикл принятия решения, приоритет качества и ограничений, критерии защиты |
| `context/Теги_хакатон.xlsx` | Листы КИП, ЛА, ПАК, ВАК. Смысл сигналов, лабораторные точки, формулы |
| `context/АВТ_схемы.pdf` | Три схемы АВТ. Отметки управлений не дают однозначного сопоставления всем CSV-тегам |
| `context/data/avt_tags.csv` | 189 217 временных точек, 71 технологический сигнал плюс время и служебные индексы |
| `context/data/242000_tags.csv` | 189 217 временных точек, 26 сигналов плюс время и индекс |
| `context/ЛИМСы 01.01.2023 - н.в_.xlsx` | 54 асинхронные серии по шести точкам. Каждая пара колонок имеет собственное время |
| `context/Выгрузка ПАК 01.01.2023 - н.в_.xlsx` | Сера в колонках A:B, расчётная D15 в D:E; разные периоды покрытия |
| `context/README.md` | Пустой, дополнительных требований нет |

Проверенные при исследовании факты, которые необходимо учитывать и воспроизвести загрузчиками:
- CSV покрывают 2023-01-01 — 2026-08-07, шаг 10 минут, временной ключ date.
- В CSV нет обычных пустот, но это не доказательство исправности: есть отрицательные расходы и повторяющиеся значения 307 в разных сигналах. Не объявлять 307 универсальным кодом ошибки без проверки.
- ПАК: 189 649 значений серы с начала 2023 года; 75 455 значений D15 начиная с марта 2025 года. Покрытие выходит за конец CSV.
- Лаборатория: 1 462 измерения серы на выходе, 132 серы сырья, 42 цетанового числа на выходе.
- Есть текстовые события Pt Created, подозрительные выбросы, ошибки подписей единиц: например, 50%.T подписан кг/м³, один D15 — °C.
- Организаторы подтвердили прямое соответствие CSV-тегов КИП, включая ht:P8 как температуру. Необычные численные значения ht:T6/ht:P8 — вопрос проверки шкал, не основание переименовать сигнал по букве. Общих коэффициентов масштабирования не предоставлено.
- Одинаковый короткий тег у АВТ и гидроочистки — разные сигналы. Обязательны пространства имён.
- Отдельных рецептур блендинга, цен ресурсов, подтверждённых промышленных диапазонов и разметки аварий в пакете не обнаружено.

### Подтверждения организаторов и обязательные изменения

Источник — ответы, переданные пользователем 2026-09-15; дата самих ответов не указана. Полный реестр формул и задач: [ORGANIZER_CLARIFICATIONS.md](../ORGANIZER_CLARIFICATIONS.md). Уточнения применять в преобразованиях, не редактировать context/.

- Подтверждены управления гидроочистки: ht:P8 — температура ГСС на входе Р-202; ht:T11 — массовый расход сырья; ht:F19 — давление на входе Р-202. ht:F26 не входит в этот подтверждённый список. Единицы/шкалы, модельные диапазоны, шаги и action support ещё надо зафиксировать до available=true.
- Соответствие CSV и КИП подтверждено. Ошибочные единицы ЛИМС исправлять по названиям показателей с сохранением original_unit, причины и источника. Исправлены пять формул ВАК гидроочистки и скобки CFPP АВТ — версии приведены в реестре, старые формулы Excel не использовать как актуальные.
- ПАК серы соответствует лабораторной точке гидроочистки 2, продукт «Дизельное топливо»; единицы ПАК подтверждены. Возраст и независимое время серий остаются обязательными.
- Организаторы сообщили, что ограничений скорости изменения параметров нет. Не выдумывать промышленные ramp limits; отсутствие ограничений не доказывает безопасность перехода. Задержку эффекта 0–3 часа исследовать самостоятельно без настройки по final test.
- Нужна собственная обоснованная модель (может быть линейной) и несколько сценариев с изменяемыми входами, чувствительных к сырью и заданию на качество. Историческое качество прогноза показывать отдельно от модельного эффекта вмешательств.
- Дополнительных материалов блендинга и полной товарной спецификации не будет. Отдельный модельный блендинг организаторы разрешили, но это не расширяет v1 автоматически: требуется решение пользователя. Численные пределы Т95/цетанового числа не предоставлены.
- Новые схемы АВТ с позициями обещаны относительно даты ответа; фактического срока получения нет. Ustanovka_AVT_merged (1).pdf и новые схемы в текущем context/ отсутствуют; не представлять их изученными.

### Непереговорные правила бизнес-логики

1. Снимок — неизменяемое состояние на момент t, включая доступную к t предысторию. Действие не изменяет исходные измерения.
2. Один цикл сравнения использует один snapshot_id, горизонт и версии модели/ограничений.
3. Пустое действие означает сохранить управляющие настройки. Измерения и качество при этом могут продолжать изменяться.
4. Сценарии — альтернативные следующие действия, не последовательность исполнения друг за другом.
5. Автоматическое и пользовательское одинаковые действия проходят одну функцию оценки и дают одинаковые расчётные результаты.
6. Входы используют только доступные на момент решения сведения. Будущий факт служит целью/проверкой и не попадает в прогноз.
7. ЛИМС — контрольный источник качества, затем ПАК, затем виртуальные анализаторы. Приоритет не означает перенос старого лабораторного значения на настоящее без учёта возраста.
8. Сера <=10 мг/кг — ограничение выбранного модельного сценария на выходе гидроочистки. Это не сертификат полного соответствия товарного топлива.
9. Экономический выигрыш не компенсирует нарушение жёстких ограничений. Текущий режим проверяется наравне с кандидатами.
10. Нет допустимых вариантов — сообщить об этом. Не советовать сохранить режим, если он тоже не прошёл проверки.
11. Неизвестная единица, непроверенное управление или неприменимая модель не превращаются в правдоподобный результат.
12. Показатель тяжести не называется вероятностью аварии. Отсутствие модели перехода к режиму не называется доказательством безопасности перехода.
13. Неподтверждённое влияние действия нельзя выдумывать ради демонстрации. Обученная прогнозная модель сама по себе не доказывает причинность.
14. Искусственные тестовые сбои и тестовые модели явно обозначены; в обычном режиме скрытого fallback на них нет.
15. Нет чата, аккаунтов, микросервисов, автоматического управления оборудованием и LLM-зависимости в критическом расчёте.

### Стек, конфигурация и хранение

- Backend: Python, FastAPI, Pydantic; pandas/numpy, openpyxl для чтения, pyarrow/Parquet; scikit-learn и joblib для модели.
- БД: PostgreSQL, SQLAlchemy 2, psycopg, Alembic. SQLite не использовать.
- Frontend: React + TypeScript + Vite; npm с package-lock.json.
- PostgreSQL локально через Docker Compose; FastAPI и Vite запускаются отдельными командами.
- Перед установкой проверить совместимость зависимостей с Python 3.14. Разработчик 1 фиксирует единую рабочую версию Python, pyproject, .python-version и uv.lock. Если нужен откат версии, синхронно обновить конфигурацию Ruff и сообщить второму разработчику.
- По умолчанию horizon_minutes=60, history_minutes=360; поддерживается только обученный горизонт. Организаторы допускают горизонт 0–3 часа и шаг рекомендаций 15–60 минут. Это не означает поддержку всех горизонтов текущей моделью/контрактом: сначала выбранные 60 минут; нулевой горизонт сейчас запрещён контрактом.
- Организаторы подтвердили общий часовой пояс источников, но не назвали зону/offset. Europe/Moscow остаётся документированным допущением; внутри UTC, отображение Europe/Moscow.
- ЛИМС measured_at — отбор пробы; до публикации проходит до 4 часов. До получения фактических времён публикации принять консервативную модельную задержку 240 минут, не выдавая её за фактическую задержку каждой пробы. Нулевая задержка и чувствительность 120 минут в текущем коде устарели: разработчик 1 должен обновить их с тестами и новой dataset version.
- Подготовленные ряды и таблицы обучения: `data/processed/`; модели и отчёты: `artifacts/`. Производные большие файлы исключить из Git.
- PostgreSQL хранит снимки, действия, оценки и решения, а не обязательную полную копию всей исходной телеметрии.
- Все записи содержат версии данных, модели и ограничений. БД не должна быть нужна для автономного обучения.
- Секрет DATABASE_URL — через окружение; в .env.example только пример. Пароли не входят в репозиторий.
- Браузер не имеет доступа к БД и не рассчитывает бизнес-метрики.

### Единые контракты contract_version=1

Владельцем `contracts.py` является разработчик 1; оба сначала согласуют его. Разработчик 2 не заводит альтернативные определения. Изменение контракта сопровождается обновлением общего примера, JSON Schema и теста сериализации.

Имена сигналов используют пространство установки, например `ht:F26`, `avt:T33`. Это примеры идентификаторов, а не подтверждение их управляемости. Лабораторные показатели дополнительно различаются точкой отбора.

| Тип | Обязательное содержание |
|---|---|
| Measurement | value: finite number или null; unit; source; measured_at; available_at; age_seconds; quality: valid/suspect/missing; issues |
| ProcessSnapshot | contract_version; snapshot_id UUID; as_of; mode: replay/manual; dataset_version; values: map сигнал→Measurement; history: записи signal_id/time/value за 6 часов; issues; completeness |
| Action | action_id UUID; label; origin: baseline/system/operator; changes: map signal_id→новое абсолютное значение. Единицы берутся из каталога, не угадываются |
| CheckResult | code; passed: true/false/null; category: data/quality/reliability/control/applicability; message; actual/limit/unit при наличии. null означает «не проверено» |
| MetricEstimate | value или null; unit; basis: measured/predicted/proxy/unavailable; explanation; lower/upper при наличии |
| QualityAssessment | target; forecast_at; prediction; lower/upper; interval_coverage_target; applicability: supported/unsupported/insufficient_data; reasons; model_version |
| ReliabilityAssessment | severity_index или null; factors с вкладом и объяснением; transition_assessed: bool; limitations |
| ScenarioEvaluation | evaluation_id UUID; snapshot_id; horizon_minutes; action; quality; reliability; throughput; cost; checks; admissibility: admissible/rejected/not_assessable; reasons; model_version; constraint_version |
| Decision | decision_id UUID; snapshot_id; horizon_minutes; status: change_recommended/no_change/insufficient_data/no_feasible_option; baseline; preferred или null; alternatives до 2; rejected evaluations; explanation; trace; версии |
| Trace entry | role; input_ids; output summary; check codes. Это журнал расчёта, не искусственная переписка и не скрытые рассуждения LLM |

IDs, origin, label не должны влиять на оценку действия. Сравнение равенства результатов в тестах игнорирует IDs и текстовые названия, но не численные результаты и проверки.

Согласованные интерфейсы:
- `SnapshotProvider.get_snapshot(at) -> ProcessSnapshot` — разработчик 1.
- `QualityAgent.assess(snapshot, action, horizon_minutes) -> QualityAssessment` — разработчик 1.
- `ReliabilityAgent.assess(snapshot, action) -> ReliabilityAssessment` — разработчик 2.
- `ScenarioEvaluator.evaluate(snapshot, action, horizon_minutes) -> ScenarioEvaluation` — разработчик 2; внутри вызывает агента качества и обычные модули проверок/эффективности.
- `Coordinator.decide(snapshot, operator_action=None, horizon_minutes=60) -> Decision` — разработчик 2; пользовательское действие включается в тот же набор кандидатов и может стать preferred.
- `DecisionRepository.save/get/list`, `SnapshotRepository.put/get` — разработчик 2; API использует их через сервисы.

Чистые вычисления не обращаются к PostgreSQL или HTTP. Сервис приложения сохраняет вход и полный результат до возвращения decision_id. Отдельное «Сохранить решение» помечает уже рассчитанное решение как выбранное для истории; оно не принимает произвольный расчёт с клиента.

### Общий HTTP-контракт

Разработчик 1 реализует FastAPI, разработчик 2 потребляет его из React. Базовый префикс `/api/v1`.

| Метод | Запрос | Ответ |
|---|---|---|
| GET /health | — | ready, model_ready, data_ready, database_ready; без секретов |
| GET /controls | — | constraint_version и каталог всех управлений с available, причиной недоступности, единицей, min/max/step, источниками допущений |
| GET /snapshots/current | — | последний снимок и current_snapshot_id |
| GET /snapshots/{id} | — | неизменяемый снимок |
| POST /replay/start | episode_id | снимок и current_snapshot_id |
| POST /replay/advance | expected_snapshot_id | следующий снимок либо 409, если состояние уже изменилось |
| GET /episodes | — | доступные реальные эпизоды, даты и ограничения; synthetic flag для тестовых |
| POST /decisions | snapshot_id, horizon_minutes=60, operator_action=null или changes | Decision и current_snapshot_id, stale |
| POST /scenarios/evaluate | snapshot_id, horizon_minutes=60, changes | ScenarioEvaluation и current_snapshot_id, stale |
| POST /decisions/{id}/save | — | saved=true; повторный вызов идемпотентен |
| GET /decisions?saved_only=true&limit=20 | — | краткая история с IDs, временем и исходом |
| GET /decisions/{id} | — | полный сохранённый расчёт и текущий признак stale |

Отдельный произвольный manual snapshot endpoint не входит в первую версию: ввод оператора — альтернативное действие на реальном снимке. Структура mode оставляет возможность расширения.

Валидация:
- некорректный JSON, неизвестный signal_id, NaN/Inf, неподдерживаемый горизонт → 422;
- несуществующий снимок/решение → 404;
- нет готовых данных или модели → 503, без тестовой подмены;
- известное управление за допустимым диапазоном → HTTP 200 с rejected и причинами, без вызова модели для недопустимого кандидата;
- нехватка данных и отсутствие допустимого решения — нормальные бизнес-ответы HTTP 200;
- ошибки имеют code, message, details.

Сравнение всегда привязано к snapshot_id запроса. stale определяется по current_snapshot_id, не временем получения HTTP-ответа. Устаревшие результаты доступны для просмотра, но явно помечены; перепривязка к новому состоянию требует пересчёта.

Replay в прототипе имеет один общий курсор для одной демонстрационной сессии. В браузере автообновление вызывает advance каждые 5 секунд только после завершения предыдущего шага. Временной шаг данных остаётся 10 минут. Это ускоренное воспроизведение, не промышленная частота опроса. При открытом редактировании альтернативы replay ставится на паузу; внешнее обновление всё равно должно распознаваться как stale.

### Согласованное владение файлами

| Область | Владелец |
|---|---|
| contracts.py, data/, quality/, api/, cli.py внутри Python package | Разработчик 1 |
| scenarios/, reliability/, optimization/, orchestration/, persistence/ внутри package | Разработчик 2 |
| config/controls.yaml, config/constraints.yaml | Разработчик 2 после совместной проверки сигналов |
| config/model.yaml, словарь сигналов, artifacts manifest | Разработчик 1 |
| frontend/, compose.yaml, alembic/, миграции | Разработчик 2 |
| pyproject.toml, uv.lock, .python-version, корневая CLI-регистрация | Разработчик 1; второй передаёт список зависимостей |
| .env.example | Разработчик 2; первый передаёт параметры API/данных |
| README.md | Разработчик 1 собирает разделы, второй передаёт команды БД/frontend |
| Общие интеграционные тесты | Разработчик 1 ведёт, разработчик 2 добавляет сценарии через согласование |

Владелец файла — способ избежать конфликтов, а не запрет обсуждать изменения. Для параллельных веток использовать codex/data-quality-api и codex/scenarios-storage-web. Не менять корневые конфиги одновременно.

### Зависимости и точки передачи

1. Совместно: контракты, пример JSON, перечень спорных тегов. Затем оба работают независимо.
2. Разработчик 1 передаёт реальный snapshot provider. Второй заменяет входной fixture на реальный снимок.
3. Разработчик 2 передаёт evaluator/coordinator, работающие с инъекцией тестового QualityAgent. Первый проверяет совместимость реальной модели.
4. Совместно утверждается каталог управлений. До этого механика генерации работает только в tests; реальные советы не считаются готовыми.
5. Разработчик 1 передаёт модель, manifest и отчёт. Второй подключает QualityAgent без переписывания собственного расчётного пути.
6. Полный цикл проверяется программно; затем первый оформляет API, второй делает UI по OpenAPI и общим примерам.
7. Совместно: запуск на чистом окружении, три демонстрационных эпизода, репетиция.

Не ждать завершения всей чужой подсистемы: ежедневно интегрировать один реальный пример. Тестовые двойники находятся только в tests и не импортируются production-кодом.

### Общие критерии готовности продукта

- Команды подготовки данных, обучения, миграций, запуска API/frontend описаны и воспроизводимы.
- Реальные данные проходят от источника до рекомендации/объяснимого отказа.
- Прогноз сравнивается с persistence baseline, final test не использовался для настройки.
- Действия, для которых нет обоснованной оценки, не получают вымышленных эффектов.
- Исходное состояние и модели сравнения одинаковы для всех вариантов.
- Operator action может стать лучшим; ограничения невозможно обойти.
- Текущий режим проверен, отказ и no_change различаются.
- Постоянные записи работают в PostgreSQL и читаются после перезапуска.
- UI не подменяет актуальное решение поздним ответом.
- Нет зависимости от внешней LLM для вычисления/объяснения.
- Демо: нормальный период; полезное изменение при наличии подтверждённого расчёта; плохие данные/ограничения. Отсутствие обоснованного эпизода полезного изменения фиксируется как незакрытый критерий, не заменяется фальшивой демонстрацией.
- Резервная запись сделана на фактической работе приложения, не на макете.

Если подтверждённых управлений или данных недостаточно, продолжать остальные независимые задачи, запросить уточнение у организаторов и явно перечислить блокированные возможности. Не выдавать прогнозный монитор за полностью завершённый оптимизатор.

## Твой маршрут реализации

### A. Зафиксировать контракты и окружение

1. Прочитать ТЗ целиком, справочник и результаты предыдущего анализа; проверить факты на исходниках.
2. Создать Pydantic-модели общего контракта в contracts.py, тест JSON round-trip и общий пример snapshot/action/evaluation/decision в examples/. Пример обязан быть помечен synthetic; числа не являются промышленными уставками.
3. Согласовать пример со вторым разработчиком до расширения собственных классов.
4. Установить согласованные Python-зависимости и зафиксировать uv.lock. Сразу проверить import всех библиотек; не выяснять совместимость с Python в конце.
5. Создать config/model.yaml с горизонтом, историей, seed=42, часовым поясом и правилами доступности источников.
6. Передать разработчику 2 contract_version=1 и интерфейс QualityAgent, чтобы он мог написать тестовый двойник.

Приёмка: uv sync воспроизводим; fixture валидируется; оба разработчика используют одни типы.

#### Фактически выполнено по пункту A (2026-09-10)

Исходные шаги пункта A выше сохранены как план и критерии приёмки. Выполненная техническая
часть зафиксирована в коде, конфигурации, общем примере и тестах:

- Полностью изучены ТЗ, справочник тегов, схемы АВТ, исходные CSV и Excel, а также результаты
  предыдущего анализа в `tmp/`. Факты повторно проверены на исходниках без изменения файлов
  `context/`.
- Проверка подтвердила, что точное значение 307 и отрицательные значения встречаются во многих
  разных сигналах. Поэтому контракт не использует числовой sentinel для качества и не превращает
  такие наблюдения в `null` без отдельного правила.
- Подтверждены противоречия справочника для `ht:T6` и `ht:P8`, ошибки подписей некоторых единиц
  ЛИМС и необходимость пространств имён. На этапе A ни одно реальное управление не объявлено
  проверенным только по имени тега, схеме или историческому диапазону.
- Общие неизменяемые Pydantic-модели `contract_version=1` реализованы в
  `src/neftecode_hackathon/contracts.py`: `Measurement`, `ProcessSnapshot`, `Action`,
  `CheckResult`, `MetricEstimate`, `QualityAssessment`, `ReliabilityAssessment`,
  `ScenarioEvaluation`, `Decision`, trace и связанные enum-типы.
- Контракт запрещает неизвестные поля, NaN/Inf и временные метки без timezone. Значение
  отсутствует только вместе с `quality=missing`; snapshot не принимает измерения или историю из
  будущего. `Decision` проверяет единые snapshot ID, горизонт, версию модели и версию ограничений
  во всех включённых сценариях.
- Создан общий end-to-end fixture `examples/contract_v1.synthetic.json` со snapshot, action,
  evaluation и decision. Он явно содержит `synthetic=true` и предупреждение, что числа и
  изменение не являются промышленными уставками.
- Из тех же моделей сгенерирована `examples/contract_v1.schema.json`. Скрипт
  `scripts/export_contract_schema.py` сначала валидирует fixture, затем воспроизводимо обновляет
  JSON Schema; повторная генерация дала идентичный SHA-256.
- Интерфейс `QualityAgent.assess(snapshot, action, horizon_minutes) -> QualityAssessment`
  зафиксирован как `Protocol` в `src/neftecode_hackathon/quality/base.py`, чтобы второй
  разработчик мог внедрить тестовый двойник без параллельных DTO и импорта tests из production.
- В `docs/CONTRACT_V1.md` собраны пути интеграции, решения контракта, правила изменения schema и
  fixture, а также команда проверки для разработчика 2. Техническая передача через общий
  репозиторий подготовлена; явное подтверждение согласования вторым разработчиком отдельно в
  репозитории пока не зафиксировано.
- В `config/model.yaml` заданы горизонт 60 минут, история 360 минут, seed 42, интерпретация
  исходного времени как `Europe/Moscow`, внутренний UTC, экспериментальные задержки источников,
  чувствительность ЛИМС 120 минут и начальные правила свежести.
- В `pyproject.toml` добавлен согласованный Python-стек для FastAPI, данных, модели и PostgreSQL;
  точные разрешённые версии сохранены в `uv.lock`. Рабочая версия Python осталась 3.14; проверка
  выполнялась на Python 3.14.2.
- `data/processed/` и `artifacts/` добавлены в `.gitignore`, чтобы производные ряды и бинарные
  модели не попадали в Git. Исходные данные и секреты не добавлялись и не изменялись.

Проверено при завершении пункта A: `uv sync --frozen`, импорт FastAPI/Pydantic, pandas/numpy,
openpyxl/PyArrow, scikit-learn/joblib и PostgreSQL-стека, детерминированная генерация JSON Schema,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest` (6 passed). Тесты
проверяли JSON round-trip, соответствие schema моделям, запрет будущей информации, смешения
snapshot ID и NaN.

### B. Реализовать загрузку и аудит

1. CSV: удалить только служебные индексы; date разобрать явно, отсортировать; проверить дубликаты и шаг. Добавить пространства avt:/ht:.
2. Excel: не читать как синхронную прямоугольную матрицу. Для ЛИМС учитывать групповые заголовки точек, показатель/единицу и отдельные пары дата-значение. Для ПАК учесть пустой разделитель C между A:B и D:E.
3. Привести анализы к записям signal_id, point_id, measured_at, available_at, value, unit, source, quality, issues. Не терять исходную подпись и исходное значение при исправлении.
4. Все времена нормализовать согласно общему timezone-допущению. Если фактического available_at нет, для ЛИМС использовать консервативное measured_at +240 минут: организаторы подтвердили отбор пробы и публикацию до 4 часов, но не задержку каждой пробы. Проверить границы доступности и чувствительность, явно обозначив допущения. Нулевая задержка/120 минут в текущем коде — прежняя политика, которую надо обновить до обучения.
5. Pt Created и другие строки записывать как события/ошибки качества, не как нули.
6. Составить data dictionary: source column, signal_id, description, original_unit, canonical_unit, conversion, verification_status, evidence. Не включать неподтверждённые единицы в физические расчёты.
7. Невозможные значения помечать по отдельным проверяемым правилам. Не удалять весь ряд из-за одного выброса; не удалять все значения 307 автоматически.
8. Сохранить telemetry.parquet, analyses.parquet, events.parquet и машинно-читаемый audit report с количеством строк, диапазонами времени, отсутствием/наличием повторов, плохими значениями и покрытиями.
9. Версию набора данных получить из хешей входов и настроек преобразования. Не менять source files.

Передача разработчику 2: словарь, audit report, список сигналов-кандидатов с подтверждённостью, реальный пример состояния.

#### Фактически выполнено по пункту B (2026-09-11)

Исходные шаги пункта B выше остаются планом и критериями приёмки. Текущее состояние
реализации подтверждено кодом, реальным запуском и тестами:

- Основной pipeline реализован в
  `src/neftecode_hackathon/data/prepare.py`; пакет экспортирует `prepare_data` через
  `src/neftecode_hackathon/data/__init__.py`. Корневой каталог `data/` предназначен для
  производных данных, поэтому Python-код в `data/processed/` не размещается.
- Добавлена явная команда `uv run neftecode-hackathon prepare`. Без подкоманды CLI показывает
  справку и не запускает тяжёлую подготовку автоматически. CLI сообщает прогресс по двум CSV,
  ЛИМС и ПАК.
- Настройки источников, сортировки, шага, chunk size, Parquet-сжатия и правил качества вынесены
  в `config/data.yaml`. Timezone, задержки и чувствительность берутся из `config/model.yaml`.
- CSV явно разбираются и сортируются по `date`; удаляются только служебные колонки
  `Unnamed: ...`; проверяются дубликаты и отклонения от шага 10 минут. Сигналы получают
  пространства имён `ht:` и `avt:`.
- ЛИМС читается как 54 независимые пары дата–значение под групповыми заголовками точек. ПАК
  читается как независимые пары A:B и D:E; непустая разделительная колонка C считается ошибкой
  структуры. Excel читается последовательным `iter_rows`, чтобы не получить нелинейное время
  на большой выгрузке ПАК.
- Все наивные исходные времена интерпретируются как `Europe/Moscow` и сохраняются в UTC.
  В записях разделены `measured_at` и `available_at`. Исходная задержка сейчас равна 0 минут;
  отдельный аудит сдвигает `available_at` всех ЛИМС-записей на 120 минут. Это эксперимент, а не
  подтверждённая задержка лаборатории.
- `Pt Created`, другие строки, нечисловые и non-finite значения не превращаются в нули: они
  сохраняются в `events.parquet`. Числовые аномалии сохраняют исходное значение и получают
  `quality=suspect` только по именованному правилу.
- Точные значения 307 только подсчитываются в аудите. Реальный результат содержит 364 593 таких
  наблюдения; они не признаны ошибочными автоматически.
- Data dictionary содержит source column, signal_id, point_id, описание, продукт, исходную и
  каноническую единицу, преобразование, verification_status и evidence. Для CSV единицы остаются
  неизвестными. `ht:T6` и `ht:P8` отмечены как suspect из-за противоречивого справочника. Ни одно
  реальное управление доступным не объявлено.
- Несовместимые подписи единиц ЛИМС не исправляются молча. Подозрительными отмечены четыре серии:
  D15 с °С, EBP.T с % об., 50%.T с кг/м3 и I350 с °С. Исходные единицы и значения сохранены.
- Создаются бинарные Apache Parquet-файлы `data/processed/telemetry.parquet`,
  `analyses.parquet`, `events.parquet`. Их сигнатуры в начале и конце равны `PAR1`, чтение через
  PyArrow проверено. Значок текстового файла в IDE является только ассоциацией редактора.
- Также создаются `audit_report.json`, `data_dictionary.json`, `candidate_signals.json` и
  `prepared_state_example.json`. Последний содержит реальное replay-состояние для передачи, но
  намеренно не называется `ProcessSnapshot`: контрактный SnapshotProvider относится к пункту C.
- Производные файлы находятся в Git-ignored `data/processed/` и воспроизводятся командой
  `prepare`; исходники `context/` не изменяются. Подробное описание схем и допущений находится в
  `docs/DATA_PREPARATION.md`.

Исторический результат полного запуска B (2026-09-11, до новых ответов организаторов;
не гарантия наличия этих артефактов в текущем checkout):

- dataset version:
  `sha256:5bb653d03082388f07c6fcc84f623ea0efb8fee1a9084b811b479a7670d3fcc3`;
- `telemetry.parquet`: 18 354 049 строк, 76 row groups;
- `analyses.parquet`: 301 904 строки, из них ЛИМС 36 800 и ПАК 265 104;
- `events.parquet`: 6 событий `Pt Created`;
- data dictionary: 153 записи;
- оба CSV: по 189 217 временных точек, без дубликатов и отклонений от шага 10 минут;
- ПАК: 189 649 значений серы и 75 455 значений D15;
- в ЛИМС выявлены два расхождения заявленного и фактического числа записей: CloudPoint 14/13 и
  CloudPoint_1 113/112;
- реальный подготовленный state содержит 153 сигнала; записей с `available_at` позже `as_of` нет.

Проверено после реализации: `uv sync`, импорт всех зависимостей, `uv run ruff check .`,
`uv run ruff format --check .`, `uv run pytest` (13 passed), полный запуск `prepare`, чтение
metadata всех трёх Parquet-файлов через PyArrow и согласованность dataset version во всех
JSON-артефактах.

### C. Реализовать SnapshotProvider

Перед пунктом C требуется корректирующее обновление B по
[ответам организаторов](../ORGANIZER_CLARIFICATIONS.md): ЛИМС timestamp —
отбор пробы, консервативный available_at +240 минут; ошибочные единицы исправить
по названиям с provenance; соответствие КИП и управляемость P8/T11/F19 подтверждены.
Переоценить прежние suspect-пометки T6/P8, не изобретая масштабирование.
Обновить конфигурацию, аудит, тесты, dataset version и производные данные.
Блоки «фактически выполнено» A/B описывают прежние запуски, не внедрение
новых подтверждений. На текущем checkout модели и SnapshotProvider ещё нет.

- Состояние строится на t; история ограничена предыдущими 6 часами.
- Любая используемая запись удовлетворяет measured_at <= t и available_at <= t.
- Для последнего анализа использовать backward as-of join по отдельной точке и показателю; не соединять по номеру строки.
- Хранить возраст значений; не делать backfill из будущего.
- Не объединять серу сырья и продукта, даже если названия показателей похожи.
- Для выбранных online-входов исходный допустимый возраст 20 минут; для ЛИМС 48 часов как экспериментальная конфигурация. Старый ЛИМС остаётся контрольным историческим фактом, но не автоматически обязательным текущим входом.
- Какие поля обязательны, задаёт manifest модели. Отсутствие обязательного входа блокирует прогноз; отсутствие дополнительного лабораторного показателя не должно безусловно блокировать все состояния.
- В случае конфликта свежих источников возвращать issue и поддерживать отказ по политике данных; величину допустимого расхождения определять по train/validation, не по final test.
- Snapshot immutable: одни и те же данные не изменяются после сохранения; новый вход создаёт новый ID.
- В тестах проверять snapshot до/после поступления лабораторного результата и на границе окна.

Приёмка: get_snapshot(t) возвращает реальное состояние без будущих данных, со всеми источниками и проблемами.

### D. Обучить прогноз продолжения процесса

1. Target: ПАК сера через 60 минут. Не интерполировать неизвестную будущую цель. Записи без корректного target исключить с подсчётом.
2. Feature builder один для обучения и serving. Признаки: доступные текущие значения выбранных достоверных сигналов, лаги 10/60/360 минут, средние за 60/360 минут и изменения за 60 минут, возраст доступных анализов.
3. Выбор сигналов обосновать до final test. Не использовать будущие фактические управляющие действия как якобы доступные сейчас признаки.
4. Разделить временную ось: 70% train, 15% validation, 15% test. Удалить из предыдущего сегмента origins, чьи targets достигают следующего сегмента. Допустимо использовать прошлую историю перед началом test; недопустимы будущие labels и fit на test.
5. Fit imputers/scalers/feature selection только на train. Причинное forward-fill допускается только в пределах заданной свежести, без глобального заполнения.
6. Baseline: последнее доступное корректное ПАК-значение серы.
7. Основная первая модель: HistGradientBoostingRegressor из scikit-learn, loss=squared_error, max_iter=200, max_leaf_nodes=15, learning_rate=0.05, l2_regularization=1.0, early_stopping=False, random_state=42. Изменения гиперпараметров выбирать только по validation.
8. На validation сравнить с baseline. Если модель хуже по MAE, для прогноза сохранения настроек выбрать baseline и явно указать результат. Persistence не используется как вымышленная модель эффекта изменения.
9. Диапазон: 90%-квантиль абсолютных validation residuals выбранного прогноза, нижняя граница ограничена нулём. Это эмпирический интервал; измерить фактическое покрытие на test, не обещать гарантированную вероятность.
10. Отчёт для validation/test: MAE, число и доля пропущенных превышений >10, число и доля ложных тревог, число превышений и нормальных точек, coverage/width интервала. При отсутствии положительных случаев доля пропусков null, не 0.
11. Отдельная проверка по ЛИМС: прогноз на timestamp пробы строится из состояния за 60 минут до неё; лабораторный результат используется только как последующий факт. Различать ошибку относительно ПАК и относительно лаборатории, показывать число совпадений и правила времени.
12. Сохранить model.joblib, manifest.json, metrics.json и короткий model report. Manifest: trained/selected-through timestamps, feature schema, horizon/history, data/config versions, обязательные входы, freshness, uncertainty method, выбранный baseline/model, ограничения.
13. Демо-эпизоды выбирать в отложенном периоде после всех данных, использованных в обучении и подборе. Сервер запрещает выдавать ретроспективный прогноз моделью, использовавшей будущее относительно replay-момента.

Приёмка: обучение повторяется фиксированной командой; есть честное сравнение; служебные события и будущая информация не вошли в features.

### E. Реализовать оценку действий вместе со вторым разработчиком

Это отдельная приёмка, не автоматический результат успешного D.

- Совместно определить до трёх управлений, проверить смысл, единицы и возможность изменять именно их. Никакой тег не получает verified=true только потому, что модель обучилась.
- Для непустого Action менять только текущие подтверждённые управления. Прошлая история неизменна. Пересчитывать производные текущие признаки; не менять независимо связанные отношения и их составляющие.
- Зафиксировать предположение о сохранении новых настроек на горизонте. Проверить, насколько наблюдательные обучающие эпизоды соответствуют этому предположению. Иначе действие not_assessable.
- Вычислять поддержку кандидата по совместному пространству выбранных физических признаков: масштабирование на train, расстояние к train-соседям, порог на train leave-one-out расстояниях. Зафиксировать k и порог в manifest. Проверять также исходное состояние. Прохождения отдельных min/max недостаточно.
- Для прогностической оценки действия явно указать observational/model-based, а не causal. Не переносить baseline interval coverage как доказанную гарантию counterfactual.
- Предоставить отдельный критерий применимости action estimate и ограничение переходной динамики. Если эффект вмешательства нельзя защитить, возвращать unsupported с причиной.
- Провести чувствительность при малых шагах, проверить знаки по технологическому обоснованию, устойчивость и отсутствие оптимизации измерения-последствия.
- Передать второму разработчику одинаковую шкалу интервала/неопределённости для сравнения качества. Если погрешность эффекта неизвестна, не выдавать точный доказанный выигрыш.
- Встречи с ментором не останавливают загрузку, прогноз, контракты и API; но блокируют включение спорного управления.

Приёмка: для каждого доступного действия есть проверяемое обоснование и support policy; unsupported возвращается штатно, без фиктивных чисел.

### F. Собрать CLI и FastAPI

Предусмотреть команды (конкретные имена фиксируются здесь):
- `uv run neftecode-hackathon prepare`
- `uv run neftecode-hackathon train`
- `uv run neftecode-hackathon evaluate --at <ISO timestamp>`
- `uv run neftecode-hackathon serve`

CLI использует те же сервисы и оценку, что API; без PostgreSQL автономно разрешены prepare/train и вывод evaluate, сохранение требует БД.

1. Подключить ScenarioEvaluator/Coordinator второго разработчика через зависимости, не реализовывать свой подбор.
2. Подключить PostgreSQL repositories второго разработчика; при API-расчёте хранить снимок и полный результат.
3. Реализовать все методы общего HTTP-контракта и OpenAPI.
4. Загружать модель один раз при старте процесса; не обучать в HTTP-запросе. CPU-расчёт не блокирует event loop.
5. Replay start/advance обрабатывать последовательно с expected_snapshot_id; локальный прототип запускается одним API-worker. Сохранять текущий replay state в PostgreSQL.
6. Не принимать произвольные пути файлов/моделей из браузера.
7. Настроить Vite proxy либо ограниченный dev CORS для согласованного localhost origin. Не открывать production wildcard credentials.
8. При отсутствии artifacts возвращать понятный 503 с указанием команды подготовки, не автоматически создавать фиктивные данные.
9. Передать frontend-разработчику OpenAPI, примеры четырёх исходов и команду запуска backend.

### G. Завершить проверку и документацию

Твои тесты:
- обе Excel-структуры разбираются независимо по времени;
- signal namespace не смешивает установки;
- delayed lab не попадает в старый snapshot;
- NaN/служебные значения не становятся нулями;
- признаки при train/serving совпадают;
- targets не пересекают границы split;
- нельзя использовать модель из будущего для replay;
- baseline/model report честно показывает проигрыш/отсутствие событий;
- unsupported action не получает прогноз пригодности;
- API соблюдает snapshot_id/horizon и статусы ошибок;
- интеграционные проверки идут на реальном PostgreSQL, а не SQLite substitute.

В README собрать команды второго разработчика и описать порядок чистого запуска: uv sync → prepare → train → PostgreSQL → alembic upgrade head → serve → npm ci/dev. Отдельно описать сборку frontend, запуск тестов, допущения, отсутствие промышленного подключения и подготовку эпизодов.

Не публиковать данные или приложение в облако по собственной инициативе.

## Что передать второму разработчику

- contracts.py + JSON Schema + синтетический общий пример.
- Реальный snapshot provider и пример снимка.
- Словарь сигналов с evidence/verification.
- QualityAgent с baseline и action paths, manifest и отчёт о применимости.
- Metrics/report, которые UI может показывать без самостоятельной интерпретации.
- OpenAPI и реальные ответы для change_recommended/no_change/insufficient_data/no_feasible_option; если какой-то исход не достигнут реальными данными, явно сказать это.
- Команды запуска, перечень ограничений и открытых технологических вопросов.

## Definition of done лично для разработчика 1

Твоя работа завершена, когда данные воспроизводимо подготовлены, прогноз проверен, оценка действий либо обоснована, либо явно блокируется, общий координатор подключён к FastAPI и PostgreSQL, API соответствует контракту, тесты проходят, frontend может работать без ручной правки ответов. Не считать готовой функцию, которая существует только в ноутбуке.
